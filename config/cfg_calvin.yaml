defaults:
  - robot: panda_longer_finger
  - scene: calvin_scene_D
  - env: env
  - aff_detection: rn50_clip_pixel
  - cameras: static_and_gripper_calvin
  - paths: general_paths
  - agent: play_lmp
  - override hydra/hydra_logging: colorlog
  - override hydra/job_logging: colorlog

data_path: ${paths.vr_data}
model_name: full

max_timesteps: 364
gripper_offset: [0.0, 0.0, -0.04]
policy_checkpoint: 
  train_folder: ./trained_agents/lfp
  model_name: epoch=30.ckpt

agent:
  viz_obs: True

depth_pred:
  checkpoint:
    train_folder: ~/logs/thesis/depth_pred/2022-04-30/18-11-11_aff_rl
    model_name: val_err.ckpt
    img_resize: 224

aff_detection:
  checkpoint:
    train_folder: ~/logs/thesis/aff_model/2022-04-29/14-02-55_aff_rl
    # train_folder: ./hydra_outputs/affordance_model/2022-03-09/01-38-55_aff_rl
    model_name: val_err.ckpt
    img_resize: 224

hough_voting:
  skip_pixels: 4
  inlier_threshold: 0.7
  angle_discretization: 100
  inlier_distance: 15
  percentage_threshold: 0.3
  object_center_kernel_radius: 10

save_dir: ./hydra_outputs/calvin
hydra:
  run:
    dir: ${save_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}